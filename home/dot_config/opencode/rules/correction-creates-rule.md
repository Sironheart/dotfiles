# Corrections become persistent rules

If you're being corrected to do something differently, create or update a rule, skill, or plugin (whichever fits) so the mistake is not repeated. Persist it in the dotfiles repo under `home/dot_config/opencode/` (`rules/`, `skills/`, `plugins/`) — never edit `~/.config/opencode/` directly, chezmoi manages it.
