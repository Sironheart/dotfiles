# Comments: why, not what

- Do **not** add comments that restate what the next lines do (e.g. policy names + field lists that mirror the code).
- Comment only when the **reason** is non-obvious: trade-offs, constraints, historical pitfalls, or why an unusual pattern exists.
- Prefer no comment over a narrating comment. If the code is clear, leave it uncommented.
