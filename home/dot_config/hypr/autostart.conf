#################
### AUTOSTART ###
#################

exec-once = 1password
exec-once = waybar &
# exec-once = systemctl --user start hyprpolkitagent
exec-once = systemctl --user start dunst
exec = gsettings set org.gnome.desktop.interface gtk-theme "flexoki"   # for GTK3 apps
exec = gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"   # for GTK4 apps
exec = gsettings set org.gnome.desktop.interface font-name "JetBrainsMono Nerd Font Mono" # set font
